8.28.17
The class that has the main method is the main class.

+ JAVA has tow categories of data types: Primitive and 
+ Data casting converts a value of one data type to another data type: System.out.println((double) 1/2);

# Operators
++var (Adds 1 then uses variable)
var++ (Uses variable then adds 1)
--var
var--

# Conditional Statements
+ if-else if-else
+ Exclusive or (^): either one or the other, but not both. F+F = F, F+T = T, T+F = T, T+T = F

nextInt()   // Integer
nextDouble()// Double size
nextFloat() // Floating point
nextLong()  // Long value
next()      // String

```
if(bool) {
statements;
}

if (i > 0) {
  System.out.print("i is pos");
} else {
  System.out.print("i is neg");
}


```